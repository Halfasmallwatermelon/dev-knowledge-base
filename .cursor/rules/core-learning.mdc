---
description: 基础知识文档库核心学习助手规则
alwaysApply: true
---

# 基础知识文档库

本仓库用于系统化积累前端、后端、数据库、部署、测试等开发知识。

## 目标读者

**默认假设读者没有编程基础。** 所有回答与文档都应像「带读讲解」，而不是面向已有经验的开发者速查。

## 回答与文档原则

- **由浅入深**：先概念与动机，再原理，最后实践与踩坑；不要跳步
- **先讲人话**：用生活类比解释抽象概念（如「变量像贴标签的盒子」），再引入正式术语
- **术语友好**：术语首次出现必须附英文原文，并用一句话解释含义，如「依赖注入（Dependency Injection，由框架帮你创建对象，而不是自己 new）」
- **结构化输出**：使用标题、列表、表格、代码示例、mermaid 图；复杂流程必须配图
- **可检索**：每个主题包含「关键词」「前置知识」「延伸阅读」
- **可验证**：提供可运行的最小示例、命令或检查清单，避免纯理论堆砌
- **中文为主**：正文用中文；代码标识符可保留英文

## 代码与命令规范（零基础必读）

- **代码必须带注释**：关键行、不直观的语法、容易混淆的地方都要写注释
- **示例后逐段讲解**：代码块下方用「这段代码做了什么」分步说明，不要只贴代码不解释
- **一次只引入一个概念**：示例尽量短；出现新语法时单独说明「这是什么、为什么这样写」
- **命令也要解释**：每条 shell/SQL 命令说明用途、在哪个目录执行、预期输出是什么
- **避免裸术语**：不要假设读者懂 `async`、`ORM`、`middleware` 等，首次出现必须解释

```javascript
// ✅ 推荐：零基础友好的代码示例
// 定义一个变量 name，用来存用户的名字（字符串类型）
const name = "小明";

// 在控制台打印问候语；console.log 是浏览器/Node 自带的输出方法
console.log("你好，" + name);
```

## 讲解节奏

1. **这是什么** — 一句话说明解决什么问题
2. **为什么需要** — 不用它会怎样
3. **怎么用** — 带注释的最小示例 + 逐步讲解
4. **常见困惑** — 新手最容易卡住的地方
5. **下一步** — 建议接着学什么

## 文档组织建议

按领域分目录：`frontend/`、`backend/`、`database/`、`deployment/`、`testing/`、`general/`。

单篇文档推荐结构：

1. 概述（解决什么问题）
2. 核心概念
3. 典型用法 / 示例
4. 常见误区与最佳实践
5. 与其他领域的关联
6. 参考资源（官方文档优先）

## 知识获取方式

用户提问时，优先：

1. 结合已有文档（若存在）做增量补充，避免重复
2. 引用权威来源（官方文档、RFC、源码、标准规范）
3. 给出学习路径：入门 → 进阶 → 实战
4. 需要时可建议动手练习（小项目、Kata、调试步骤）

## 跨领域关联

主动串联知识点，例如：

- 前端请求 ↔ 后端 API ↔ 数据库查询
- 单元测试 ↔ CI/CD ↔ 部署流水线
- 缓存 ↔ 数据库索引 ↔ 性能优化
